# people-news

```bash
npm install
pip3 install flask

python ./api/api.py # start server for ner
python ./scrapers/index.js # start scraper
python hrlex.py ./lex/hrLexFile ./lex/out.txt # process hrlex file
```

```javascript
db.posts.createIndex( { titleHash: 1 }, { unique: true } ) // create index, do this inside mongo console
```
